// The main homepage renders the V8 "Two Otters Studio" design (the version we're shipping).
// Kept as a re-export so `/` and `/v8` stay in sync from a single source.
export { default } from './v8/page'
