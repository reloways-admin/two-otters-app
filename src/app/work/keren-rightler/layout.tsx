import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Keren Rightler - Two Otters case study',
  description: 'Keren Rightler - case study coming soon.',
}

export default function KerenRightlerLayout({ children }: { children: React.ReactNode }) {
  return children
}
